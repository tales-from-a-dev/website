{% extends 'base.html.twig' %}

{% block meta_title %}
    {{ 'tag.title'|trans({tag: tag}) }} | {{ parent() }}
{% endblock %}

{% block body %}
    <h1>{{ 'tag.title'|trans({tag: tag}) }}</h1>
    <div>
        {% for post in tag.posts %}
            {{ include('website/blog/_post_short.html.twig') }}
        {% endfor %}
    </div>
{% endblock %}
