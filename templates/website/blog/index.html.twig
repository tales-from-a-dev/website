{% extends 'base.html.twig' %}

{% block meta_title %}
    {{- 'blog.title'|trans }} | {{ parent() -}}
{% endblock %}

{% block body %}
    {{ form(form) }}

    {% for post in posts %}
        {% include 'website/blog/_post_short.html.twig' %}
    {% endfor %}

    {{ knp_pagination_render(posts) }}
{% endblock %}
